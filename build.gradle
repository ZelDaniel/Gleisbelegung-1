/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/3.4.1/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'application'
apply plugin: 'jacoco' // java coverage report
//apply plugin: 'javafx-gradle-plugin'

sourceCompatibility = 1.8
targetCompatibility = 1.8
mainClassName = 'org.gleisbelegung.Plugin'
version = '0.0.1'

/*
buildscript {
    dependencies {
        classpath group: 'de.dynamicfiles.projects.gradle.plugins', name: 'javafx-gradle-plugin', version: '8.8.2'
    }
}
*/

sourceSets {
    main {
        java {
            srcDirs = ['src/main/java']
        }
        resources {
            srcDirs = ['src/main/resources']
        }
    }
    test {
        java {
            srcDirs = ['src/test/java']
        }
        resources {
            srcDirs = ['src/test/resources']
        }
    }
}

/*
jfx {
    // minimal requirement for jfxJar-task
    mainClass = 'Main'

    // minimal requirement for jfxNative-task
    vendor = 'ich'

    // some optional task
    jfxAppOutputDir = 'build'
    jfxMainAppJarName = 'Gleisbelegung'
    manifestAttributes = [
        "Specification-Version": 1.0,
        "Implementation-Version": 1,
        "Built-By": "greonyral",

    ]
    // for a full list of available settings, look the class "JavaFXGradlePluginExtension" on plugin project
}
*/

jar {
    manifest {
        attributes(
                "Implementation-Title": "StS Plugin Gleisbelegung",
                "Implementation-Version": version,
                'Main-Class': mainClassName
        )
    }
}

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
    mavenLocal()
    mavenCentral()
}

// In this section you declare the dependencies for your production and test code
dependencies {
    testCompile 'junit:junit:4.12'
}

jacocoTestCoverageVerification {
    violationRules {
        rule {
            element = 'CLASS'
            includes = [
              'org.gleisbelegung.utils.*'
            ]
            limit {
                minimum = 0.9
            }
        }
        rule {
            element = 'CLASS'
            includes = [
              //'org.gleisbelegung.collection.*',
              'org.gleisbelegung.sts.*',
            ]
            excludes = [
              // specified by following rule
              'org.gleisbelegung.sts.TrainType',
              'org.gleisbelegung.sts.ScheduleEntry.StsScheduleEntry',
            ]
            limit {
                minimum = 0.8
            }
        }
        rule {
            // reaches 39% coverage but with 0 missed lines
            element = 'CLASS'
            includes = [
              'org.gleisbelegung.sts.TrainType',
            ]
            limit {
                minimum = 0.38;
            }
        }
        rule {
            element = 'CLASS'
            enabled = false
            includes = ['org.gleisbelegung.ui']
            limit {
                counter = 'LINE'
                minimum = 0.3
            }
        }
        rule {
            element = 'CLASS'
            enabled = false
            includes = ['org.gleisbelegung.*']
            limit {
                minimum = 0.4
            }
        }
    }
}
